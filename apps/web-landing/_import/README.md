# Import staging

Copy your desktop `SMART_Landing_Page` project files here first, then refactor into `../src/components/landing/`.

This folder is excluded from TypeScript (`tsconfig.json`) and is not deployed.
