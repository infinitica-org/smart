import { Card, CardDescription, CardHeader, CardTitle } from '@smart/ui';

export default function Page() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Platform Health & Metrics</CardTitle>
        <CardDescription>Metrics dashboard placeholder.</CardDescription>
      </CardHeader>
    </Card>
  );
}
