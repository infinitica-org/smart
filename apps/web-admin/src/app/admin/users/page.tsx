import { Card, CardDescription, CardHeader, CardTitle } from '@smart/ui';

export default function Page() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>User & Role Directory</CardTitle>
        <CardDescription>User and institution management placeholder.</CardDescription>
      </CardHeader>
    </Card>
  );
}
