import { Card, CardDescription, CardHeader, CardTitle } from '@smart/ui';

export default function Page() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Webhook Integrations</CardTitle>
        <CardDescription>Outbound CRM integration targets placeholder.</CardDescription>
      </CardHeader>
    </Card>
  );
}
