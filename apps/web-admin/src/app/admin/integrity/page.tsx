import { Card, CardDescription, CardHeader, CardTitle } from '@smart/ui';

export default function Page() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Integrity Review Queue</CardTitle>
        <CardDescription>Proctoring review queue placeholder.</CardDescription>
      </CardHeader>
    </Card>
  );
}
