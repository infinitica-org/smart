/** Native <select> menus ignore many Tailwind text colors; force a readable OS list. */
export const nativeSelectClass =
  'h-9 w-full min-w-0 rounded-lg border border-[var(--surface-border,rgba(15,23,42,0.16))] bg-white px-2 text-xs text-slate-900';

export const nativeOptionClass = 'bg-white text-slate-900';
