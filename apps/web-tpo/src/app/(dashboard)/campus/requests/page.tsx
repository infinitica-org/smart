import { EmployerRequestsQueue } from '../../../../components/campus/EmployerRequestsQueue';

export const metadata = { title: 'Employer requests · SMART TPO' };

/** UNI-05 (Th6-445/446) — employers requesting campus access. */
export default function EmployerRequestsPage() {
  return <EmployerRequestsQueue />;
}
