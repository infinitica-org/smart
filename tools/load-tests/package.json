{
  "name": "@smart/load-tests",
  "version": "0.2.0",
  "private": true,
  "description": "k6 performance/stress-testing suite for the whole SMART stack (k6 CLI required, or use the Docker fallback — see README). Owner: Vishal V.",
  "scripts": {
    "test": "bash scripts/require-k6.sh && k6 run src/tests/smoke.js",
    "typecheck": "echo skip",
    "lint": "echo skip",
    "clean": "rimraf summary-*.json"
  },
  "devDependencies": {
    "rimraf": "6.0.1"
  }
}
