# Kubernetes / Helm

Sprint 0 ships **Docker Compose on a VPS**. Helm charts land when the pilot outgrows a single node.

Do not invent a second production path before Compose is stable.

Owner: Vishal V.
